[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pytest-greet"
version = "0.1.0"
classifiers = ["Framework :: Pytest"]

[project.entry-points.pytest11]
greet = "pytest_greet.plugin"