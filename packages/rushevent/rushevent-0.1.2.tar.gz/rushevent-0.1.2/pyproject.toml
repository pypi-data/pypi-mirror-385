[build-system]
requires = ["setuptools >= 48.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rushevent"
version = "0.1.2"
description = "python event"
authors = [
    {name = "ndrzy", email = "dandan0019@outlook.com"}
]
readme = {file = "README.md", content-type = "text/markdown"}
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = [
    "pygame-ce"
]

[project.urls]
Homepage = "https://github.com/meatdumplings0019/rushevent"

[tool.setuptools]
packages = {find = {}}