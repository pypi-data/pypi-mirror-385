# Environment Configuration Example
# Copy this file to .env and adjust as needed

# Environment type: local, development, staging, production, testing
ENVIRONMENT=local

# Optional: Custom banner text (overrides default)
# BANNER_TEXT="CUSTOM ENVIRONMENT MESSAGE"

# Optional: Custom banner color in hex format
# BANNER_COLOR="#FF5722"
