[tool.poetry]
name = "ams-sdk"
version = "1.0.7"
description = "Python SDK for the Alternative Macro Signals API"
authors = ["Laurent Bilke <laurent@alternativemacrosignals.com>"]
readme = "README.md"
include = [
    "logo_icon_small_tw.jpg",
    "LICENSE",
]
#requires-python = ">=3.9,<3.13"

[tool.poetry.dependencies]
#python = "^3.7"
#poetry = "^2.0.0"
requests = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
