# VS Code MCP Configuration Template
# Purpose: Template for .vscode/mcp.json (VS Code agent configuration)
# Variables: {{SERVER_NAME}}, {{COMMAND}}, {{ARGS}}, {{ENV_KEY}}, {{ENV_VALUE}}
# Generated by: mcp-config-generator agent
# Template location: ~/.multiagent/mcp/templates/.vscode-mcp.json.template
#
# IMPORTANT: VS Code requires hardcoded API key values (not ${VAR} references)
# The mcp-config-generator agent replaces {{ENV_VALUE}} with actual key from ~/.bashrc

{
  "mcpServers": {
    "{{SERVER_NAME}}": {
      "command": "{{COMMAND}}",
      "args": [{{ARGS}}],
      "env": {
        "{{ENV_KEY}}": "{{ENV_VALUE}}"
      }
    }
  }
}
