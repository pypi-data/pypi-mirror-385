# Claude Code MCP Configuration Template
# Purpose: Template for .mcp.json (Claude Code agent configuration)
# Variables: {{SERVER_NAME}}, {{COMMAND}}, {{ARGS}}, {{ENV_KEY}}, {{ENV_VALUE}}
# Generated by: mcp-config-generator agent
# Template location: ~/.multiagent/mcp/templates/.mcp.json.template
#
# IMPORTANT: Claude Code requires hardcoded API key values (not ${VAR} references)
# The mcp-config-generator agent replaces {{ENV_VALUE}} with actual key from ~/.bashrc

{
  "mcpServers": {
    "{{SERVER_NAME}}": {
      "command": "{{COMMAND}}",
      "args": [{{ARGS}}],
      "env": {
        "{{ENV_KEY}}": "{{ENV_VALUE}}"
      }
    }
  }
}
