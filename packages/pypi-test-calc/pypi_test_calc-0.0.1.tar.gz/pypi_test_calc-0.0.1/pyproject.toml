[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pypi-test-calc"
version = "0.0.1"
description = "Test calculator package"
authors = [{name = "Your Name"}]
readme = "README.md"
license = {text = "MIT"}
keywords = ["calculator", "test"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = []

[project.urls]
Homepage = "https://github.com"

