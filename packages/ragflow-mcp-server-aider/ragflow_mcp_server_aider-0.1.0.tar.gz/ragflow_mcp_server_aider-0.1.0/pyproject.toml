[project]
name = "ragflow-mcp-server-aider"
version = "0.1.0"
description = "RAGFlow MCP Server Aider"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
 "mcp>=1.18.0",
 "ragflow-sdk>=0.21.0",
]

[build-system]
requires = [ "uv_build>=0.9.4,<0.10.0",]
build-backend = "uv_build"

[project.scripts]
ragflow-mcp-server-aider = "ragflow_mcp_server_aider:main"
