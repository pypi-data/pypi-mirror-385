# Copy this file to .env and customize the values before running docker compose.

# ========================================
# n8n Configuration
# ========================================
N8N_ENCRYPTION_KEY=replace-this-with-a-long-random-string
TZ=UTC
GENERIC_TIMEZONE=UTC
N8N_API_KEY=your-api-token

# ========================================
# mcp-n8n Gateway Configuration
# ========================================

# Environment Mode
# Set to 'dev' for local development or 'stable' for production
MCP_N8N_MODE=stable

# ========================================
# Stable Configuration (Production)
# ========================================
# Used when MCP_N8N_MODE=stable
# Uses published mcp-n8n package from PyPI

# Logging (Stable)
MCP_N8N_LOG_LEVEL=INFO

# ========================================
# Development Configuration
# ========================================
# Used when MCP_N8N_MODE=dev
# Uses local editable install (pip install -e)

# Development Paths (Update to match your system)
# DEV_MCP_N8N_PATH=/Users/youruser/code/mcp-n8n
# DEV_CHORA_PATH=/Users/youruser/code/chora-compose
# DEV_CODA_PATH=/Users/youruser/code/mcp-server-coda

# Logging (Dev) - More verbose for debugging
# DEV_MCP_N8N_LOG_LEVEL=DEBUG
# DEV_MCP_N8N_DEBUG=1

# ========================================
# Backend API Keys (Required for Both Modes)
# ========================================

# Backend: Chora Composer
# Required for code generation and AI-powered content generation
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Backend: Coda MCP
# Required for all Coda operations
# Get from: https://coda.io/account
CODA_API_KEY=your_coda_api_key_here

# ========================================
# Optional Configuration (Both Modes)
# ========================================

# Folder for creating new Coda documents
CODA_FOLDER_ID=your_coda_folder_id_here

# Gateway Behavior
# Enable debug logging for MCP protocol (0=off, 1=on)
MCP_N8N_DEBUG=0

# Backend subprocess timeout (seconds)
MCP_N8N_BACKEND_TIMEOUT=30

# ========================================
# Usage Notes
# ========================================
#
# For Stable Mode:
#   1. Set MCP_N8N_MODE=stable
#   2. Install: pip install mcp-n8n
#   3. Add API keys above
#   4. Start: mcp-n8n
#
# For Dev Mode:
#   1. Set MCP_N8N_MODE=dev
#   2. Uncomment DEV_* paths and update them
#   3. Install: cd /path/to/mcp-n8n && pip install -e .[dev]
#   4. Add API keys above
#   5. Start via IDE or: python -m mcp_n8n.gateway
#
# See .config/README.md for detailed configuration instructions
