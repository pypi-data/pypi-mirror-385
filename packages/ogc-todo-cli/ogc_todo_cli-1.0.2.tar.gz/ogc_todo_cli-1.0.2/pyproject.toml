[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ogc-todo-cli"
version = "1.0.2"
description = "A simple command-line To-Do manager built in Python"
authors = [{ name="Nwaokolo Godstime", email="lionartogc.com@gmail.com" }]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
todo = "todo_cli.cli:task_menu"
