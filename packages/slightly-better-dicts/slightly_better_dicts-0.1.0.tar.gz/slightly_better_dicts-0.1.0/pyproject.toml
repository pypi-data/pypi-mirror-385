[project]
name = "slightly-better-dicts"
version = "0.1.0"
description = "A Python library that provides some classes and functions that add quality of life improvements to the standard library's `dict`"
readme = "README.md"
authors = [
    { name = "Matt Hale", email = "matthew.hale@protonmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.3,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
