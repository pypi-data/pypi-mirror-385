# Pixverse MCP Configuration File
# 这是 MCP 程序配置，包含服务器设置和默认值
# 用户必须在 ~/.cursor/mcp.json 中配置 PIXVERSE_API_KEY 环境变量

# API Configuration
base_url: "https://app-api.pixverse.ai"  # MCP程序配置，用户无需修改
# 注意：api_key 必须通过环境变量 PIXVERSE_API_KEY 提供
