[project]
name = "nexus-tool"
version = "0.1.1"
description = "Local-first terminal toolkit for cryptography, OSINT, log analysis, and enumeration"
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.11"
dependencies = ["click", "duckdb", "pyarrow", "pandas"]

[project.scripts]
nexus = "nexus.cli:cli"

[project.urls]
Homepage = "https://github.com/H4ch1Net/Nexus"
Issues = "https://github.com/H4ch1Net/Nexus/issues"