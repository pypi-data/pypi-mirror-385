[project]
name = "nexus-tool"   # choose an available name on PyPI
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["click","duckdb","pyarrow","pandas"]

[project.scripts]
nexus = "nexus.cli:cli"
