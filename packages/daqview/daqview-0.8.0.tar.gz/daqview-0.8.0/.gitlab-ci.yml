before_script:
  - python -V
  - curl -LsSf https://astral.sh/uv/install.sh | sh
  - uv --version

test:
  script:
    - uv run pytest -v --cov=daqview daqview
    - uv run flake8 daqview
    - uv run pylint --rcfile=pylint.rc daqview || true
  coverage: '/^TOTAL.*\s+(\d{1,3}\.\d{2}%)$/'
