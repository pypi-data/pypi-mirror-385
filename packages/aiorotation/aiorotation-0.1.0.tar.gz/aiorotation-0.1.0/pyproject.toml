[build-system]
requires = ['setuptools', 'wheel']
build-backend = 'setuptools.build_meta'

[project]
name = 'aiorotation'
version = '0.1.0'
description = 'Asynchronous library for easy requests to APIs with any rate limits, with support for token rotation.'
readme = 'README.md'
requires-python = '>=3.8'
license = { text = 'MIT' }
authors = [
    { name = 'Creeper Hack' }
]
dependencies = []

[project.urls]
Homepage = 'https://github.com/creehk/aiorotation'
