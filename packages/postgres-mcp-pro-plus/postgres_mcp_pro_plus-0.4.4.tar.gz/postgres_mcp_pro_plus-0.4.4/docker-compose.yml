services:
  postgres-mcp:
    build: .
    container_name: postgres-mcp
    ports:
      - "8099:8099"
    env_file:
      - .env
    networks:
      - cloud-cost-optimization_default

networks:
  cloud-cost-optimization_default:
    external: true
