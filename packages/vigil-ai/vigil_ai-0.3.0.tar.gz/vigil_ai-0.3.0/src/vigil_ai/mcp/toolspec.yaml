verbs:
  # Foundation Layer - Core Vigil operations
  - name: preview_data
  - name: run_target
  - name: promote

  # Application Layer - AI-powered operations
  - name: ai_generate_pipeline
  - name: ai_debug_error
  - name: ai_optimize_workflow
  - name: ai_generate_step
  - name: ai_list_models
  - name: ai_get_model_info

# Keep in sync with scopes in .vigil/workspace.spec.json and resolver defaults.
scopes: [
  # Foundation layer
  preview_data,
  run_target,
  promote,
  # Application layer (AI)
  ai_generate_pipeline,
  ai_debug_error,
  ai_optimize_workflow,
  ai_generate_step,
  ai_list_models,
  ai_get_model_info,
]
