[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyatmoweb"     
version = "0.0.3"
description = "PyAtmoWeb helpers"
readme = "readme.md"
authors = [{ name="Enno Henn", email="henn@ipfdd.de" }]
requires-python = ">=3.8"
dependencies = [
  "requests"
]
