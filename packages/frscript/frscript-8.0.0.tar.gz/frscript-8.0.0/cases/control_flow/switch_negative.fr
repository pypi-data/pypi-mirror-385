// !default

void main() {
    int x = -1
    switch (x) {
        case 0:
            println("zero")
        case 1:
            println("one")
        default:
            println("default")
    }
}
