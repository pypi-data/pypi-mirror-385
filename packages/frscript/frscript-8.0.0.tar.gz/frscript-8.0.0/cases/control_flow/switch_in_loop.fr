// !zero\none\ntwo

void main() {
    for (i in 0..3) {
        switch (i) {
            case 0:
                println("zero")
            case 1:
                println("one")
            case 2:
                println("two")
            default:
                println("other")
        }
    }
}
