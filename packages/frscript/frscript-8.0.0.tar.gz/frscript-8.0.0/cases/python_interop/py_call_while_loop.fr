// !1.0
// !2.0
// !4.0

py_import math

void main() {
    int i = 0
    while (i < 3) {
        float result = py_call("math", "pow", 2.0, float(i))
        println(str(result))
        i = i + 1
    }
}
