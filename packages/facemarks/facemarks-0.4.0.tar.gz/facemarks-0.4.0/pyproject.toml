[project]
name = "facemarks"
version = "0.4.0"
description = "Prediction of 3d facemarks on a 3d face mesh."
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "mediapipe>=0.10.21",
    "open3d==0.18.0",
]

[tool.setuptools.packages.find]
where = ["src"]
