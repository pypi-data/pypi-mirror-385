[tool.poetry]
name = "api-pipedrive-python"
version = "1.3"
description = "API wrapper for Pipedrive written in Python"
authors = ["Weriqui Ferreira de Souza <weriquetiao@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "pipedrive"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

