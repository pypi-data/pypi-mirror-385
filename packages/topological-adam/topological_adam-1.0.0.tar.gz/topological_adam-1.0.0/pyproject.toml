[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "topological-adam"
version = "1.0.0"
description = "Energy-Stabilized Topological Adam Optimizer (MHD-inspired)"
authors = [{name = "Steven Reid"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["torch>=1.10"]
