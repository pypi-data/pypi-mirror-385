[build-system]
requires = ["uv_build >= 0.9.2, <0.10.0"]
build-backend = "uv_build"

[project]
name = "formforge"
version = "0.0.0"
authors = [
  { name = "Thomas Lovén", email = "formforge@thomasloven.com"},
]
description = "Dataclasses with coercion, and structured input format exports"
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]
licence = "MIT"
licence-files = ["LICEN[SC]E*"]

[project.urls]
Homepage = "https://github.com/thomasloven/formforge"
