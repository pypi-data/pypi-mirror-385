[build-system]
requires = [
    "setuptools>=61.2",
    "Cython",
    "setuptools-cmake-helper",
]
build-backend = "setuptools.build_meta"

[project]
name = "test-package"
description = "Helper for testing."
requires-python = ">=3.8"
dependencies = []
version = "0.0.1"
