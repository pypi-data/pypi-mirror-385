[build-system]
requires = ["hatchling>=1.20"]
build-backend = "hatchling.build"

[project]
name = "sanityscan-dhruva"
version = "0.1.0"
description = "CSV sanity report + stratified train/val/test splits."
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["pandas", "numpy", "scikit-learn"]
