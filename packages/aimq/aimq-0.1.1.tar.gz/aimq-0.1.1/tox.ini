[tox]
envlist = py311, py312
isolated_build = True

[testenv]
skip_install = false
allowlist_externals = uv
commands_pre =
    uv sync --frozen --group dev
commands =
    uv run pytest {posargs:tests}
