[project]
name = "iGSTT"
version = "0.1.0"
description = "Gemini STT CLI"
authors = [
    {name = "MLeo",email = "me@mleo.site"}
]
readme = "docs/README.md"
keywords = ["LRC", "Gemini"]
include = ["LICENSE"]
license = {text = "MIT"}
requires-python = ">=3.13"

dependencies = [
    "google-genai (>=1.36.0,<2.0.0)"
]

[project.scripts]
igstt = "igstt:main"

[project.urls]
Homepage = "https://stt.mleo.site"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
