[project]
name = "parsesafe"
version = "0.1.2"
description = "A type-safe parsing combinator library."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
pythonpath = [ "src" ]
testpaths = [ "tests" ]