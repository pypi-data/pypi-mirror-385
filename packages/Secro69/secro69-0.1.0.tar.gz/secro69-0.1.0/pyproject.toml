[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Secro69"
version = "0.1.0"
description = "Simple downloader library by EI3RAB"
readme = "README.md"
authors = [{name="Abdullah Ali"}]
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = []
