[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dost_cat"
version = "0.1.0"
description = "Easy a DOS"
authors = [
  { name = "HebeleKedi", email = "muhammedefekayha@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"