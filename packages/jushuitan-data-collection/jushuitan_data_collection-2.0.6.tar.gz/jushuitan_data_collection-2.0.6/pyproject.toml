[project]
name = "jushuitan-data-collection"
version = "2.0.6"
description = "Jushuitan & Shengsuan Data Collection"
authors = [{ name = "Martian Bugs" }]
requires-python = ">=3.12"
dependencies = ["browser-automation-launcher>=2.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/JushuitanDataCollection"]
