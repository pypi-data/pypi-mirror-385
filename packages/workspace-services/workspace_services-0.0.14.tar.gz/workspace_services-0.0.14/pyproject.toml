[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "workspace_services"
version = "0.0.14"
description = "Workspace Services"
readme = "README.md"
requires-python = ">=3.12"
classifiers = ["Programming Language :: Python :: 3", "Operating System :: OS Independent"]
dependencies = ["rich", "scikit-learn"]
[[project.authors]]
name = "Sheldon Lee"
email = "sheldonlee@outlook.com"

[project.optional-dependencies]
dev = ["build", "pytest", "pytest-env", "mypy", "pre-commit"]

[tool.sys-dependencies]
apt = []

[tool.setuptools.packages.find]
where = ["src/"]
include = ["workspace_services*", "workspace_services/*"]
