[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py98"
version = "0.1.0"
authors = [
    { name="higashiikenaga", email="example@example.com" }
]
description = "Python-based PC-98 BIOS & System Emulator"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []
