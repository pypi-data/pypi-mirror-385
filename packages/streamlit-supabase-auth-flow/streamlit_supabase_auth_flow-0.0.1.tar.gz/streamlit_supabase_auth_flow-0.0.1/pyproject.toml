[project]
name = "streamlit-supabase-auth-flow"
version = "0.0.1"
description = "Simple Supabase auth for streamlit using implicit flow."
readme = "README.md"
authors = [
    { name = "Adamos Spanashis", email = "adamingas@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.48.1",
    "streamlit-url-fragments>=0.1.2",
]

[build-system]
requires = ["uv_build>=0.8.5,<0.9.0"]
build-backend = "uv_build"
