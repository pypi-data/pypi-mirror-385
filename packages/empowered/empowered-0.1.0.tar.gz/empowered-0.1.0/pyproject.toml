[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "empowered"
version = "0.1.0"
description = "EmpowerEd project utilities and API"
authors = [
    { name="Na'Sir Miller" }
]

[tool.setuptools]
packages = ["empowered"]