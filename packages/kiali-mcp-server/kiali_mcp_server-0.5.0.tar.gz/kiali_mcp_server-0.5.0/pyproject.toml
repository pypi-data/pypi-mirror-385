[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kiali-mcp-server"
version = "0.5.0"
description = "Kiali MCP Server (Model Context Protocol) with special support for OpenShift"
readme = {file="README.md", content-type="text/markdown"}
requires-python = ">=3.6"
license = "Apache-2.0"
authors = [
    { name = "Marc Nuri", email = "marc@marcnuri.com" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/kiali/kiali-mcp-server"
Repository = "https://github.com/kiali/kiali-mcp-server"

[project.scripts]
kiali-mcp-server = "kiali_mcp_server:main"
