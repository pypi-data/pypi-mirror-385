[project]
name = "slack-github-triager-core"
version = "0.3.3"
description = "Core library for Slack-GitHub triaging functionality"
authors = [{name = "Eli Block", email = "3347571+eliblock@users.noreply.github.com"}]
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "pyjwt[crypto]>=2.10.1"
]

[project.optional-dependencies]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
