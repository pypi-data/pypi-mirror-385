[build-system]
requires = ["setuptools >= 48.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rushconsole"
version = "0.1.1"
description = "python console"
authors = [
    { name = "ndrzy", email = "dandan0019@outlook.com" }
]
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    'questionary'
]

[project.urls]
Homepage = "https://github.com/meatdumplings0019/rushconsole"

[tool.setuptools]
packages = { find = { } }