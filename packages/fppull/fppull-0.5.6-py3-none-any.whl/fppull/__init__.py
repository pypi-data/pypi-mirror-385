try:
    # Prefer the file generated by setuptools-scm's `write_to`
    from ._version import version as __version__
except Exception:
    # Fallback if running from a source tree without _version.py
    try:
        from importlib.metadata import version

        __version__ = version("fppull")
    except Exception:
        __version__ = "0.0.0"
