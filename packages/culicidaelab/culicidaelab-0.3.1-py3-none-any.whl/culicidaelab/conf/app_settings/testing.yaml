base_dirs:
  user_data: "../testing/user_data"
  cache: "../testing/cache"
  models: "../testing/models"
  datasets: "../testing/datasets"
  downloads: "../testing/downloads"
  archives: "../testing/archives"

# Mapping to ConfigManager's get_resource_dirs() method
resource_mapping:
  user_data_dir: "${base_dirs.user_data}"
  cache_dir: "${base_dirs.cache}"
  model_dir: "${base_dirs.models}"
  dataset_dir: "${base_dirs.datasets}"
  downloads_dir: "${base_dirs.downloads}"
  archived_files_dir: "${base_dirs.archives}"
